
## 开始

```bash
git clone https://github.com/ericwang915/PythonClaw.git
cd PythonClaw
python -m venv .venv && source .venv/bin/activate
pip install -e .
pytest tests/ -v
```

## 添加新技能

1. 在 `pythonclaw/templates/skills/<category>/<skill_name>/` 下创建目录。
2. 添加 `SKILL.md` 文件，包含对代理的清晰指令。
3. 添加任何支持脚本。
4. 提交 PR。

## 添加新的LLM

1. 在 `pythonclaw/core/llm/` 中创建新文件。
2. 实现 `base.py` 中的 `LLMProvider` 接口。
3. 在 `main.py:_build_provider()` 中添加提供者选项。
4. 添加测试和文档。

## 添加新渠道

1. 在 `pythonclaw/channels/` 中创建新文件。
2. 使用 `SessionManager` 进行代理生命周期管理和并发控制。
3. 遵循 `telegram_bot.py` 模式（对异步处理程序使用 `sm.acquire(sid)`）。
4. 将渠道连接到 `server.py`、`main.py` 和 `onboard.py`。
